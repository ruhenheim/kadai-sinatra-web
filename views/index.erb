<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8">
    <title>タイトル</title>
  </head>
  <body>
    <form action="/" method="post" >
      名前: <input type="text" name="target_name">
      年齢: <input type="text" name="target_age">
      <input type="submit" value="決定">
    </form>

    <% name = params[:target_name] %>
    <% age = params[:target_age] %>
    <% time_japan = Time.now.getlocal("+09:00") %>

    <% if 6 <= time_japan.hour && time_japan.hour < 12 %>
      <% hello_word = 'おはよう' %>
    <% elsif 12 <= time_japan.hour && time_japan.hour < 18 %>
      <% hello_word = 'こんにちは' %>
    <% else %>
      <% hello_word = 'こんばんわ' %>
    <% end %>

    <p><%= hello_word %><%= "、#{name}(#{age})さん" if name && age %></p>
    <p>今は<%= time_japan.hour %>時です。</p>
  </body>
</html>